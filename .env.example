# ===========================================
# Personal Library Environment Configuration
# ===========================================
# This is a template file. Copy this to .env and fill in the actual values.
# Never commit the .env file to version control.

# Database Configuration
# ----------------------
# Password for SQL Server SA account (must meet SQL Server password requirements)
# Requirements: At least 8 characters, including uppercase, lowercase, numbers, and special characters
SA_PASSWORD=YourStrong@Passw0rd

# Database host name (use 'db' for Docker, 'localhost' for local development)
DB_HOST=db

# API Configuration
# -----------------
# URL where the frontend will connect to the API
# For Docker: http://localhost:5274
# For local development: http://localhost:5000 or your API URL
VITE_API_URL=http://localhost:5274

# ===========================================
# Usage Notes
# ===========================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Update the values in .env with your actual configuration
# 3. For production, use strong passwords and secure the .env file
# 4. Never commit .env to version control (already in .gitignore)
